* Реализовать page object
* BasePage
    * `url()`
    * `open()`
    * `getPage()`
* Для каждого наследника переопределить `url()`
* Создать поля с нужными элементами страницы
* Сделать геттеры для созданных элементов (?)
* Сделать методы с поведением созданных элементов
* Вынести админские креды в проперти
    * `admin.username=admin`
    * `config.getProperty("admin.username")`
* Сделать метод создания объекта CreateUserRequest с админскими кредами (но наверно имелось ввиду AuthLoginRequest)
* Для тестов, где мы не проверяем функционал логина, сделать логин по локалсторедж: `executeJavaScript("localStorage.setItem('authToken', arguments[0]);", authToken);` и тут же:
    * Сделать метод в рекспектах для получения токена авторизации
* Сделать BaseUiTest
    * Сделать перегруженный метод (по логину паролю и по модели сразу) с открытием baseurl и авторизации
    * Вынести конфиг настройки Selenide в проперти
        * Изменить нейминг для api baseurl
    * Унаследоваться от BaseTest?
* Можно сделать интерфейс или абстрактный класс что то вроде LoginPasswordForm Authable c дефолтными методами ввода пассворда и логина и объединить страницы **не забыть**
    * Не забыть модификатор доступа протектед
    * Но Саша добавила эти методы сразу в BaseUiTest
* Аналогичное решение с интерфейсом Aдля Аллертов **не забыть**
    * Но Саша добавила эти методы сразу в BaseUiTest
    * Можно добавить сразу в тело метода ассерт аллерта
* Сделать enum для текстов аллертов
    * Конструктор который сетапит поле енама
    * Поле строка константа с текстом аллерта
* Сделать getAll - но у меня уже есть